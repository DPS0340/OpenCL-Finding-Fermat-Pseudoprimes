#pragma once
#include <iostream>
#include <vector>

namespace Index {
    void distribute(std::vector<int> startIdx, std::vector<int> endIdx, size_t local_size, int startFind, int endFind)
    {
        for (int n = startFind; n < endFind; n++) {
            int task_length = n*
        }
    }
}